<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <% content_for :title, "Gastos" %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Gastos</h1>
    <%= link_to "Novo Gasto", new_expense_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div id="expenses" class="min-w-full">
    <table class="table table-lg table-pin-rows table-pin-cols mx-auto">
      <thead>
        <tr>
          <td>Descrição</td>
          <td>Valor</td>
          <td>Data</td>
          <td>Fixo?</td>
        </tr>
      </thead>
      <tbody>
          <% @expenses.each do |expense| %>
          <tr>
            <td><%= expense.description %></td>
            <td><%= format_brl_currency(expense.value) %></td>
            <td><%= format_date(expense.date) %></td>
            <td><%= format_boolean(expense.fixed) %></td>
            <td class="w-28">
              <%= link_to "Ver", expense, class: "btn btn-info w-fit" %>
            </td>
          </tr> 
          <% end %>
      </tbody>
      <tfoot>
        <tr>
          <td>Descrição</td>
          <td>Valor</td>
          <td>Data</td>
          <td>Fixo?</td>
        </tr>
      </tfoot>
    </table>
  </div>
</div>
